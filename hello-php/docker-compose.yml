version: "3"

networks:
  webnet:

services:
  nginx:
    image: nginx:1.17.3-alpine
    ports:
      - "8080:80"
    networks:
      - webnet
    volumes:
      - "./config/site.conf:/etc/nginx/conf.d/default.conf"
      - "./www:/www"